run the builder.bash file to build the project
Create a terminal instance at the folder containing "Conjuga.jar"
Run "java -jar Conjuga.jar"
To edit the rules and choices open the Choices folder and follow the existing format
